services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: careerforge-pg
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: careerforge
    ports:
      - "5432:5432"
    volumes:
      - careerforge-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d careerforge"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  careerforge-pgdata:
